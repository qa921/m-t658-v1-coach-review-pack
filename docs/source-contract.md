# Source contract

Authoritative row-level facts are in Supabase `m_t658_v1.player_kpi_source`. Stored destinations are `snapshot_records`; only rows marked `correction_authorized` may be corrected. `Harbour Cup U19` is intentionally unresolved. Low-volume cases remain visible but ineligible at the approved threshold. Match summaries must use `BR-APR-2026-A` only. Formula references in source rows point to the expected OneDrive workbook sheet and row; formula comparison remains pending until that workbook exists.
